---
description: "Never write TypeScript to lib/ directory"
alwaysApply: lib/**/*.ts
priority: high
---

# Never Build to lib/

## Rule

**NEVER compile or build TypeScript output to `lib/` directories.**

Build output (transpiled JavaScript, source maps, declaration files) must go to:
- `dist/` (preferred)
- `build/`
- `out/`

## If You See lib/*.ts Files

1. Move them to the right spot. 
